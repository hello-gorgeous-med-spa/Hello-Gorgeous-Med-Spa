/**
 * @deprecated Public booking uses Fresha (`lib/booking/fresha-booking-url.ts`).
 * Square is retained for in-spa payments / POS only.
 */
export {
  bookingUrlForService,
  freshaBookingUrlForService,
} from "@/lib/booking/fresha-booking-url";
