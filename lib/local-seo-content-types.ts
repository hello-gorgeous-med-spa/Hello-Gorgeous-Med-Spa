/**
 * Shared shape for location landing pages (GBP + med-spa city pages).
 */

export type LocationContentSection = {
  intro: string;
  aboutTreatment: string;
  candidacy: string;
  whatToExpect: string;
  safetyAndTraining: string;
  communityContext: string;
  callToAction: string;
};
