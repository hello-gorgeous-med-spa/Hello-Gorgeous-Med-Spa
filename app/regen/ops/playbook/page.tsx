import { RegenStaffBible } from "@/components/regen/RegenStaffBible";

export const metadata = {
  title: "REGEN RX Staff Bible",
  robots: { index: false, follow: false },
};

export default function OpsPlaybookPage() {
  return <RegenStaffBible />;
}
