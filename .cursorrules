# Cursor workspace – work from main only

Work from the **main git clone** of this repo (e.g. `~/Hello-Gorgeous-Med-Spa/hello-gorgeous-med-spa`). Do not use a worktree or mirror path for editing; commit and push only from the main clone.
